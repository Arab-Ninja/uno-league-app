<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Prise de photo de profil (PHOTO-001).

        L'écran de photo passe par `getUserMedia` dans la WebView. Sans cette
        déclaration, Android refuse la caméra **en silence** : aucune boîte de
        dialogue n'est présentée, et l'application affiche « L'accès à la
        caméra a été refusé » comme si l'utilisateur avait dit non.

        `required="false"` est délibéré : une tablette sans caméra doit pouvoir
        installer l'application. L'écran propose alors de choisir un fichier.
        Le mettre à `true` retirerait l'application du Play Store pour tous ces
        appareils, sans qu'on s'en aperçoive.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!--
        Notifications push (ANN-004). Obligatoire depuis Android 13 : sans
        elle, l'abonnement réussit et aucune notification n'est jamais
        affichée.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
